import 'package:flutter/material.dart';

class BizJonundo extends StatelessWidget {
  const BizJonundo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('biz')),
    );
  }
}
